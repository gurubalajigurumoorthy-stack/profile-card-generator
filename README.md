👤 User Profile Card Generator

A simple full-stack web application that generates a user profile card from user inputs. Users can enter their name, bio, and image URL, and the backend processes the data to display a formatted profile card.

Features
Enter name, bio, and image URL
Generate a profile card instantly
Backend data processing using Flask
Responsive and clean UI